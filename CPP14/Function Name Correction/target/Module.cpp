#include <std>

void __HelloWorld()
{
    // Lets pretend we have some body here
}

void __HelloPerson()
{
    // Even if we are pretending the function has body with comments
    // the AST Parser can also parse comments if we wanted
}
